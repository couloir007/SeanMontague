{{ attach_library('surface/surface-elevation-profile') }}

<div class="surface-elevation-profile{{ modifier ? ' ' ~ modifier }}"
     data-once="elevation-profile"
     {% if map_id %}data-map-id="{{ map_id }}"{% endif %}
     {% if elev_data %}data-elev='{{ elev_data|json_encode }}'{% endif %}>
  <div class="surface-elevation-profile__header">
    <span class="surface-elevation-profile__header-label">Elevation Profile</span>
    <div class="surface-elevation-profile__header-stats">
      {% if gain %}
        <div class="surface-elevation-profile__stat">
          <span class="surface-elevation-profile__stat-val">{{ gain }}</span>
          <span class="surface-elevation-profile__stat-unit">ft gain</span>
        </div>
      {% endif %}
      {% if loss %}
        <div class="surface-elevation-profile__stat">
          <span class="surface-elevation-profile__stat-val">{{ loss }}</span>
          <span class="surface-elevation-profile__stat-unit">ft loss</span>
        </div>
      {% endif %}
      {% if min_elev %}
        <div class="surface-elevation-profile__stat">
          <span class="surface-elevation-profile__stat-val">{{ min_elev }}</span>
          <span class="surface-elevation-profile__stat-unit">ft min</span>
        </div>
      {% endif %}
      {% if max_elev %}
        <div class="surface-elevation-profile__stat">
          <span class="surface-elevation-profile__stat-val">{{ max_elev }}</span>
          <span class="surface-elevation-profile__stat-unit">ft max</span>
        </div>
      {% endif %}
    </div>
  </div>
  <canvas class="surface-elevation-profile__canvas" id="{{ canvas_id|default('elevation-chart') }}"></canvas>
  <div class="surface-elevation-profile__tooltip" id="{{ tooltip_id|default('elev-tooltip') }}"></div>
</div>
