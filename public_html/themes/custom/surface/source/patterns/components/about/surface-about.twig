{{ attach_library('surface/surface-about') }}

<section class="surface-about{{ modifier ? ' ' ~ modifier }}" id="about">
  {% if label %}
    <span class="surface-about__label">{{ label }}</span>
  {% endif %}
  <div class="surface-about__grid">
    <div>
      {% if title %}
        <h2 class="surface-about__aside-title">{{ title|raw }}</h2>
      {% endif %}
      {% if skills %}
        <ul class="surface-about__skills">
          {% for skill in skills %}
            <li>{{ skill }}</li>
          {% endfor %}
        </ul>
      {% endif %}
    </div>
    <div class="surface-about__body">
      {% if paragraphs %}
        {% for p in paragraphs %}
          <p>{{ p }}</p>
        {% endfor %}
      {% endif %}
    </div>
  </div>
</section>
